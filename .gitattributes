# Datasets and trained models — tracked via Git LFS so the repo stays small.
# Run `git lfs install` once on each clone before pulling.
api/data/** filter=lfs diff=lfs merge=lfs -text
*.csv filter=lfs diff=lfs merge=lfs -text
*.idx3-ubyte filter=lfs diff=lfs merge=lfs -text
*.idx1-ubyte filter=lfs diff=lfs merge=lfs -text
*.npz filter=lfs diff=lfs merge=lfs -text
